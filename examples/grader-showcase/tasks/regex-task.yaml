# Task: Demonstrate text grader
# Tests the agent's ability to produce output matching specific patterns
id: regex-demo-001
name: Text Pattern Matching Demo
description: |
  This task demonstrates the text grader, which validates that the agent's
  output matches (or doesn't match) specific substrings and regular expression patterns.
  
  The agent is asked to list programming languages, and we verify the output
  contains expected patterns and excludes unwanted ones.

tags:
  - text
  - pattern-matching
  - demo

inputs:
  prompt: "List 3 popular programming languages for web development"
  context:
    task_type: information_query

expected:
  # Expected patterns we'd like to see
  output_contains:
    - "Python"
    - "JavaScript"

# Task-specific graders
graders:
  # Text grader validates output patterns
  - name: language_patterns
    type: text
    config:
      # Regex patterns that MUST match in the output
      regex_match:
        - "(?i)(python|javascript|typescript|ruby|php|java)"
        - "(?i)(web|development|backend|frontend)"
      # Regex patterns that MUST NOT match
      regex_not_match:
        - "(?i)error|exception|failed"
        - "(?i)COBOL|Fortran"  # Unlikely for web dev!
